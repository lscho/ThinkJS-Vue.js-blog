module.exports = {

    /**
     * hook
     * @param  {[type]} node 钩子点
     * @param  {[type]} data 数据
     * @return {[type]}      [description]
     */
    async hook(node, data) {
        try {

        } catch (e) {
            think.logger.error(e);
        }
    }
}